# Copy this file to .env and point it at your dev machine's LAN IP so
# physical devices on the same network can reach the Go backend.
# Find your LAN IP with: ip addr show | grep 'inet ' | grep -v 127.0.0.1
EXPO_PUBLIC_API_URL=http://192.168.1.23:8080
