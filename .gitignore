# Go
server/data/
server/mu3ic-server

# Node / Expo
app/node_modules/
app/.expo/
app/dist/
app/.env
app/.env.local

# Deployment secrets / local backup artifacts
deploy/.env
deploy/backups/
# `neon env pull` writes live branch credentials into one of these at the repo root
/.env
/.env.local

# Editors / OS
.DS_Store
.neon
*.swp
*.swo
*~
