# Phase 9: Transcoding (Deferred)

**Status:** Planned — build only if needed

## Goal

On-demand transcoding via ffmpeg. Only implement if adaptive bitrate becomes necessary — see the [HTTP range requests decision](DECISIONS.md#http-range-requests-for-audio-streaming) for why this is deferred rather than scaffolded now.
